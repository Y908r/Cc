name: Multi

on:
  push:
    branches:
      - main

jobs:

   run-on-ubuntu1:
    runs-on: ubuntu-latest
    if: always()  # This ensures the job runs even if previous jobs fail
    

    steps:
     - name: checkout code
       uses: actions/checkout@v4.0.0-beta1
     - name: Setup PHP
       uses: shivammathur/setup-php@v4.0.0-beta1
       with:
        php-version: 7.10
     - name: Run Php Program
     
       run :
          php a.php
          
          

